# Browser/server
HOST=0.0.0.0
PORT=3000
PUBLIC_PORT=3000
LOG_LEVEL=info
# Required whenever production listens beyond loopback. Use 24+ random
# characters and enter the same token in the browser unlock screen.
APP_AUTH_TOKEN=replace-with-a-long-random-demo-token

# Volcengine Ark. ARK_MODEL is an endpoint/model ID that supports the
# OpenAI-compatible Responses API, for example ep-xxxxxxxx.
ARK_API_KEY=replace-with-your-ark-api-key
ARK_MODEL=ep-replace-with-your-endpoint-id
ARK_BASE_URL=https://ark.cn-beijing.volces.com/api/v3

# Runtime paths (Docker defaults are already correct).
APP_DATA_DIR=/app/data
AGENT_WORKSPACE_ROOT=/app/workspaces
CODEX_HOME=/app/codex-home
CODEX_BIN=codex

# Compose/ECS runs Codex inside the shared application container. This image
# does not probe Landlock or automatically downgrade workspace-write.
# Only scripts/start-local-poc.sh probes and may fall back to danger-full-access
# inside its disposable runtime container. Never apply that fallback to an
# uncontained host process. Use scoped demo credentials and no unrelated data.
CODEX_SANDBOX_MODE=workspace-write
CODEX_TIMEOUT_MS=600000
CODEX_MAX_OUTPUT_BYTES=2097152

# Runtime policy. Commands the Agent asks the Runtime to execute are evaluated
# as they are observed; a denial requests cancellation via the selected runner.
# This is not pre-execution approval or guaranteed descendant-process cleanup.
# POLICY_RULES overrides the built-in rules with a JSON array of
# {id, description, asset, pattern}.
POLICY_ENABLED=true
# POLICY_RULES=

# Trace retention. Event spans are capped per Run, and older Runs are dropped
# whole so a retained trace never keeps half a tree.
TRACE_MAX_EVENT_SPANS_PER_RUN=500
TRACE_RETENTION_RUNS=200

# Token prices used for the audit bundle's cost estimate, per million tokens.
# Left at 0 the platform reports no cost rather than a misleading 0.00; set
# them to your endpoint's published rates to price a Run. Codex reports
# cached input tokens as a subset of input tokens, so the cached half is
# billed once, at the cached rate.
TRACE_COST_CURRENCY=USD
TRACE_COST_INPUT_PER_MTOK=0
TRACE_COST_CACHED_INPUT_PER_MTOK=0
TRACE_COST_OUTPUT_PER_MTOK=0

# Runtime provider. The ECS/Compose image uses local-process because Codex is
# installed in the application container. scripts/start-local-poc.sh overrides
# this to container and starts a disposable Docker/Colima/Podman Runtime.
RUNTIME_PROVIDER=local-process
CONTAINER_ENGINE=docker
CONTAINER_RUNTIME_IMAGE=volc-agent-runtime:local
# Optional build mirrors for networks that cannot reach Docker Hub or
# deb.debian.org. Leave empty on normal networks.
CONTAINER_RUNTIME_BASE_IMAGE=node:22-bookworm-slim
CONTAINER_APT_MIRROR=
CONTAINER_APT_SECURITY_MIRROR=
# Optional extra tools, for example append: python3 build-essential
CONTAINER_RUNTIME_APT_PACKAGES=ca-certificates git ripgrep
CONTAINER_CPU_LIMIT=2
CONTAINER_MEMORY_LIMIT=2g
CONTAINER_PIDS_LIMIT=256
RUNTIME_INSTANCE_ID=default
# CONTAINER_USER is filled with the host UID:GID by start-local-poc.sh.
# CONTAINER_USER=1000:1000
